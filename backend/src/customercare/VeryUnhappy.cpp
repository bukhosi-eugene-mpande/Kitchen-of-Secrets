#include "VeryUnhappy.h"

VeryUnhappy::VeryUnhappy(std::shared_ptr<Customer> customer) : SatisfactionState("VeryUnhappy", customer, 0.3, 0.0){

}

void VeryUnhappy::console(){
    /**
     * @brief customer is already very unhappy, so no need to change mood so implement the leave function here
     * 
     */
}

void VeryUnhappy::anger(){
    /**
     * @brief customer is already very unhappy, so no need to change mood so implement the leave function here
     * 
     */
}